# ============================================
# Mini-Medallion: CPU-only Dependencies
# ============================================
# Install this along with requirements-base.txt for CPU-only environments.
#
# Usage: pip install -r requirements-base.txt -r requirements-cpu.txt

# --- PyTorch (CPU) ---
torch>=2.2.0 --index-url https://download.pytorch.org/whl/cpu
torchvision>=0.17.0 --index-url https://download.pytorch.org/whl/cpu
torchaudio>=2.2.0 --index-url https://download.pytorch.org/whl/cpu
